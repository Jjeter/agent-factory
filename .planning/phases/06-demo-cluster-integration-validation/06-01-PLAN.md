---
phase: 06-demo-cluster-integration-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_factory_cli.py
  - tests/test_demo_artifact.py
autonomous: true
requirements:
  - TDD-RED-gate-approve-logs-demo-artifact
must_haves:
  truths:
    - "All new Phase 6 test stubs exist and xfail (not skip, not error)"
    - "Full test suite collects and runs without import crashes"
    - "Wave 1 tasks (approve/logs) have a named failing test for each behavior"
    - "Wave 2 tasks (demo, artifact) have named failing tests for existence checks"
  artifacts:
    - path: "tests/test_factory_cli.py"
      provides: "xfail stubs for approve + logs + demo subcommands"
      contains: "test_approve_cluster_not_found"
    - path: "tests/test_demo_artifact.py"
      provides: "xfail stubs for artifact structure and DB seeding checks"
      contains: "test_cluster_db_seeded"
  key_links:
    - from: "tests/test_factory_cli.py"
      to: "runtime.cli.factory_cli"
      via: "pytest.importorskip"
      pattern: "importorskip.*runtime.cli"
---

<objective>
Establish the TDD RED gate for Phase 6 — add xfail stubs for all new behaviors before
any implementation begins.

Purpose: Nyquist compliance — every future implementation task turns its stubs GREEN.
Output: tests/test_factory_cli.py (9 new stubs) + tests/test_demo_artifact.py (new file, 5 stubs).
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-demo-cluster-integration-validation/06-CONTEXT.md
@.planning/phases/06-demo-cluster-integration-validation/06-VALIDATION.md
</context>

<interfaces>
<!-- Key imports and patterns from existing test files -->
<!-- Use pytest.importorskip INSIDE the test body (not at module level) — project invariant -->

From tests/test_factory_cli.py (existing structure — append new stubs):
```python
import pytest
from click.testing import CliRunner

def test_create_returns_immediately(): ...  # existing — do not modify
def test_status_in_progress(): ...          # existing — do not modify
# ... other existing tests ...
```

From runtime.cli (existing factory_cli group):
- `factory_cli` is a click.Group with subcommands: create, list, status, add-role
- New subcommands to stub tests for: approve, logs, demo
</interfaces>

<feature>
  <name>Phase 6 TDD RED Gate</name>
  <files>tests/test_factory_cli.py, tests/test_demo_artifact.py</files>
  <behavior>
    Stubs to add to tests/test_factory_cli.py:
    1. test_approve_cluster_not_found — approve exits 0 with info message when cluster DB does not exist
    2. test_approve_wrong_state — approve exits 0 with info message when task is not in 'review' state
    3. test_approve_success — approve updates task to 'approved' and prints success output
    4. test_logs_cluster_not_found — logs exits 0 with info message when cluster DB does not exist
    5. test_logs_table_output — logs prints a tabulate "simple" table with timestamp/agent_id/action/details columns
    6. test_logs_json_output — logs --json prints a JSON list
    7. test_logs_tail — logs --tail N limits output rows
    8. test_logs_agent_filter — logs --agent <id> filters rows to one agent
    9. test_demo_exists — agent-factory demo subcommand exists (exit 2 = unknown command; stub checks it does not exit 2)

    New file tests/test_demo_artifact.py (5 stubs):
    1. test_demo_artifact_docker_compose_exists — clusters/demo-date-arithmetic/docker-compose.yml exists
    2. test_demo_artifact_launch_sh_exists — clusters/demo-date-arithmetic/launch.sh exists
    3. test_demo_artifact_env_example_exists — clusters/demo-date-arithmetic/.env.example exists
    4. test_cluster_db_seeded — clusters/demo-date-arithmetic/db/cluster.db has >= 2 rows in agent_status
    5. test_readme_exists — README.md exists at project root
  </behavior>
  <implementation>
    All stubs use @pytest.mark.xfail(strict=False, reason="not yet implemented").
    Each stub body: import runtime.cli via pytest.importorskip (for CLI stubs) or just assert False with a clear message (for artifact stubs that check filesystem).
    test_demo_artifact.py does NOT use importorskip — it checks filesystem paths, no module imports needed.
    All stubs must be collected and xfail (not error) when run against the current codebase.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approve + logs + demo stubs to tests/test_factory_cli.py</name>
  <files>tests/test_factory_cli.py</files>
  <behavior>
    - test_approve_cluster_not_found: invokes factory_cli ["approve", "no-such-cluster", "task-abc"] with isolated FACTORY_CLUSTERS_BASE; asserts exit_code == 0 and "not found" in output
    - test_approve_wrong_state: creates a real tmp cluster DB with a task in 'todo' state; invokes approve; asserts exit_code == 0 and "not" in output (state mismatch message)
    - test_approve_success: creates tmp cluster DB with a task in 'review' state; invokes approve; asserts exit_code == 0 and "Approved" in output
    - test_logs_cluster_not_found: invokes factory_cli ["logs", "no-such-cluster"] with isolated FACTORY_CLUSTERS_BASE; asserts exit_code == 0 and "not found" in output
    - test_logs_table_output: creates tmp cluster DB with one activity_log row; invokes logs; asserts exit_code == 0 and column headers in output
    - test_logs_json_output: creates tmp cluster DB with one activity_log row; invokes logs ["logs", cluster_name, "--json"]; asserts valid JSON list in output
    - test_logs_tail: creates tmp cluster DB with 5 activity_log rows; invokes logs ["logs", cluster_name, "--tail", "2"]; asserts only 2 rows in output
    - test_logs_agent_filter: creates tmp cluster DB with 2 agents' entries; invokes logs --agent boss-01; asserts only boss-01 rows appear
    - test_demo_exists: invokes factory_cli ["demo", "--help"]; asserts exit_code == 0 (help text exists means subcommand registered)
  </behavior>
  <action>
    Append 9 new test functions to tests/test_factory_cli.py. All marked @pytest.mark.xfail(strict=False, reason="not yet implemented").

    Each test that needs a cluster DB uses a helper that:
    1. Creates a tmp cluster dir structure: tmp_path / "clusters" / "test-cluster" / "db" / "cluster.db"
    2. Seeds the DB with schema using sqlite3 stdlib (read from "runtime/schema.sql") — this avoids async complexity in test helpers
    3. Invokes runner with env={"FACTORY_CLUSTERS_BASE": str(tmp_path / "clusters")}

    test_approve_wrong_state seeds a task with status='todo' (not 'review').
    test_approve_success seeds a task with status='review'.
    test_logs_* tests seed the activity_log table with test rows.

    Use stdlib sqlite3 (not aiosqlite) for DB seeding in test helpers — avoids asyncio.run() inside test setup.
    Schema path: Path("runtime/schema.sql").read_text() — the test runner CWD is the project root.

    Keep existing tests UNCHANGED. Append only.
  </action>
  <verify>
    <automated>python -m pytest tests/test_factory_cli.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 new stubs collect and xfail. All existing 7 tests remain GREEN. Full file runs without import or collection errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_demo_artifact.py with artifact structure stubs</name>
  <files>tests/test_demo_artifact.py</files>
  <behavior>
    - test_demo_artifact_docker_compose_exists: Path("clusters/demo-date-arithmetic/docker-compose.yml").exists() is True
    - test_demo_artifact_launch_sh_exists: Path("clusters/demo-date-arithmetic/launch.sh").exists() is True
    - test_demo_artifact_env_example_exists: Path("clusters/demo-date-arithmetic/.env.example").exists() is True
    - test_cluster_db_seeded: connect to clusters/demo-date-arithmetic/db/cluster.db; SELECT count(*) FROM agent_status; assert >= 2
    - test_readme_exists: Path("README.md").exists() is True (project root)
  </behavior>
  <action>
    Create tests/test_demo_artifact.py as a new file.

    No importorskip needed — tests check filesystem paths and a sqlite3 connection only.
    All 5 functions marked @pytest.mark.xfail(strict=False, reason="not yet implemented").

    Use pathlib.Path for all path checks. For test_cluster_db_seeded, use stdlib sqlite3:
    ```python
    import sqlite3
    conn = sqlite3.connect("clusters/demo-date-arithmetic/db/cluster.db")
    count = conn.execute("SELECT count(*) FROM agent_status").fetchone()[0]
    conn.close()
    assert count >= 2
    ```

    The tests are relative to the project root (pytest CWD). Use bare Path("clusters/...") — pytest runs from the project root directory.

    Add a module docstring: "Structural tests for the committed demo cluster artifact."
  </action>
  <verify>
    <automated>python -m pytest tests/test_demo_artifact.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 stubs collect and xfail. File runs without import errors. Full suite still GREEN: python -m pytest tests/ -q 2>&1 | tail -5</done>
</task>

</tasks>

<verification>
After both tasks:
- python -m pytest tests/ -q shows all pre-existing GREEN tests still GREEN
- 9 new stubs in test_factory_cli.py collected as xfail
- 5 stubs in test_demo_artifact.py collected as xfail
- No new test file causes collection errors
- Total xfail count increases by 14; no new failures or errors
</verification>

<success_criteria>
- tests/test_factory_cli.py: 9 new xfail stubs for approve (3), logs (4), demo (1) + demo_exists (1)
- tests/test_demo_artifact.py: new file with 5 xfail stubs
- Full suite collects and runs: python -m pytest tests/ -q exits 0
- Existing 128 GREEN tests remain GREEN
</success_criteria>

<output>
After completion, create .planning/phases/06-demo-cluster-integration-validation/06-01-SUMMARY.md
</output>
