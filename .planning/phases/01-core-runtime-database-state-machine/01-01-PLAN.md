---
phase: 01-core-runtime-database-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/__init__.py
  - runtime/schema.sql
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_database.py
  - tests/test_models.py
  - tests/test_state_machine.py
autonomous: true
requirements:
  - schema.sql (7 tables, WAL-native, CREATE TABLE IF NOT EXISTS)
  - test infrastructure (conftest, fixtures, all test stubs)

must_haves:
  truths:
    - "pytest discovers all test files without import errors"
    - "All 7 tables are declared in schema.sql with CREATE TABLE IF NOT EXISTS"
    - "schema.sql uses parameterized defaults only — no f-string SQL"
    - "WAL pragma is set in schema.sql preamble"
    - "Test fixtures provide an in-memory db connection and factory helpers"
  artifacts:
    - path: "runtime/__init__.py"
      provides: "runtime package marker"
    - path: "runtime/schema.sql"
      provides: "canonical DDL for all 7 tables"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "tests/__init__.py"
      provides: "tests package marker"
    - path: "tests/conftest.py"
      provides: "async db fixture + create_goal/create_task helpers"
    - path: "tests/test_database.py"
      provides: "stubs for DB-01 through DB-05, CLI-01, CLI-02"
    - path: "tests/test_models.py"
      provides: "stubs for MDL-01 through MDL-03, SM-04"
    - path: "tests/test_state_machine.py"
      provides: "parametrized transition table stubs for SM-01 through SM-03"
  key_links:
    - from: "tests/conftest.py"
      to: "runtime/database.py"
      via: "DatabaseManager import (will exist in Plan 04)"
      pattern: "from runtime.database import DatabaseManager"
    - from: "runtime/schema.sql"
      to: "runtime/database.py"
      via: "Path(__file__).parent / 'schema.sql'"
      pattern: "CREATE TABLE IF NOT EXISTS"
---

<objective>
Create the package structure, canonical schema, and full test scaffold for Phase 1.

Purpose: Every subsequent plan's automated verify command depends on test files existing. This plan writes them first (Wave 0 pattern). The schema.sql is also foundational — database.py reads it by relative path, so it must exist before Plan 04 can be verified.

Output: `runtime/` package marker, `runtime/schema.sql` with all 7 tables, `tests/` package with conftest + 3 test files. All tests import correctly and either SKIP or XFAIL (not ImportError) until implementation plans run.
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-runtime-database-state-machine/01-CONTEXT.md
@.planning/phases/01-core-runtime-database-state-machine/01-RESEARCH.md

<interfaces>
<!-- Key contracts the executor must implement. -->

From pyproject.toml (already exists):
- Entry point: `cluster = "runtime.cli:cluster_cli"`
- Entry point: `agent-factory = "runtime.cli:factory_cli"`
- pytest asyncio_mode = "auto" — no @pytest.mark.asyncio needed
- --cov-fail-under=80 is active — test files must not be empty stubs that tank coverage

From CONTEXT.md (locked):
- DB backend for tests: `:memory:` SQLite per test — NOT temp files
- Test fixtures: function-scoped (NOT session-scoped) to prevent state bleed
- conftest.py helpers: `create_goal()` and `create_task()` called per-test
- Transition table pattern: `@pytest.mark.parametrize` over `(from_state, to_state, should_succeed)` tuples
- `rejected` does NOT appear as a TaskStatus enum value

From RESEARCH.md:
```python
# conftest.py async fixture pattern
@pytest.fixture
async def db():
    manager = DatabaseManager(Path(":memory:"))
    conn = await manager.open_write()
    await manager.init_schema(conn)
    yield conn
    await conn.close()
```

Schema tables (7 required):
  goals, tasks, task_comments, task_reviews, agent_status, documents, activity_log
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime package and canonical schema.sql</name>
  <files>runtime/__init__.py, runtime/schema.sql</files>
  <action>
Create `runtime/__init__.py` as an empty package marker (just a module docstring: `"""Agent Factory runtime package."""`).

Create `runtime/schema.sql` with all 7 tables using `CREATE TABLE IF NOT EXISTS`. The file must contain EXACTLY the DDL from REQUIREMENTS.md §4, with these additions and constraints:

1. Open with WAL pragma preamble (these run on `db up`):
```sql
PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;
```

2. All 7 tables in dependency order (goals before tasks, tasks before task_comments/task_reviews/documents/activity_log, agent_status last):
   - `goals` (id TEXT PK, title, description, status DEFAULT 'active', created_at DEFAULT datetime('now'))
   - `tasks` (id TEXT PK, goal_id FK→goals.id, title, description, assigned_to TEXT, status DEFAULT 'todo', priority INTEGER DEFAULT 50, model_tier DEFAULT 'haiku', escalation_count INTEGER DEFAULT 0, stuck_since TEXT, created_at, updated_at)
   - `task_comments` (id TEXT PK, task_id FK→tasks.id, agent_id, comment_type, content, created_at)
   - `task_reviews` (id TEXT PK, task_id FK→tasks.id, reviewer_id, status DEFAULT 'pending', created_at, UNIQUE(task_id, reviewer_id))
   - `agent_status` (id TEXT PK, agent_role, status DEFAULT 'idle', last_heartbeat TEXT, current_task TEXT REFERENCES tasks(id))
   - `documents` (id TEXT PK, task_id FK→tasks.id, title, content, version INTEGER DEFAULT 1, created_by, created_at)
   - `activity_log` (id TEXT PK, agent_id, task_id REFERENCES tasks(id), action, details TEXT, created_at)

3. All TEXT fields NOT NULL unless explicitly nullable per schema (assigned_to, stuck_since, last_heartbeat, current_task, task_id in activity_log, details are nullable).
4. No f-string interpolation, no dynamic SQL — pure static DDL.
5. Use `CREATE TABLE IF NOT EXISTS` for every table (idempotent for `db up`).

Add useful indexes at the end:
```sql
CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks(status);
CREATE INDEX IF NOT EXISTS idx_tasks_goal_id ON tasks(goal_id);
CREATE INDEX IF NOT EXISTS idx_activity_log_agent ON activity_log(agent_id);
```
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; sql = Path('runtime/schema.sql').read_text(); tables = ['goals','tasks','task_comments','task_reviews','agent_status','documents','activity_log']; missing = [t for t in tables if f'CREATE TABLE IF NOT EXISTS {t}' not in sql]; assert not missing, f'Missing tables: {missing}'; print('schema.sql OK')"</automated>
  </verify>
  <done>runtime/__init__.py exists as package marker. runtime/schema.sql contains all 7 tables with CREATE TABLE IF NOT EXISTS, WAL pragma preamble, and indexes. Python one-liner validates all table names present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffold — conftest, test_database, test_models, test_state_machine</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_database.py, tests/test_models.py, tests/test_state_machine.py</files>
  <behavior>
    - conftest.py `db` fixture yields a connected in-memory aiosqlite connection with schema initialized; connection is closed after each test
    - conftest.py `create_goal(db)` async helper inserts a goal row and returns its id string
    - conftest.py `create_task(db, goal_id)` async helper inserts a task row linked to the given goal_id and returns its id string
    - test_database.py: test functions exist and are importable for DB-01 (open_write returns connection), DB-02 (WAL mode = 'wal'), DB-03 (busy_timeout = 5000), DB-04 (foreign_keys = 1), DB-05 (init_schema creates all 7 tables), CLI-01 (db up idempotent), CLI-02 (db reset drops and recreates)
    - test_models.py: test functions exist for MDL-01 (Goal round-trips), MDL-02 (Task rejects invalid status), MDL-03 (all 7 models construct without error), SM-04 (rejected not in TaskStatus values)
    - test_state_machine.py: single parametrized test_transition function covering all valid and invalid transitions from the TRANSITION_CASES table
    - All test functions either xfail with `pytest.importorskip` or use `pytest.mark.xfail(reason="not yet implemented")` so the suite collects without ImportError before Plans 02-04 exist
  </behavior>
  <action>
Create `tests/__init__.py` as empty package marker.

Create `tests/conftest.py`:
- Import guard: wrap `from runtime.database import DatabaseManager` in a try/except ImportError, skip fixtures gracefully if runtime not yet installed
- `db` fixture: function-scoped async fixture. Opens DatabaseManager(Path(":memory:")), calls open_write(), calls init_schema(conn), yields conn, closes conn in finally
- `create_goal(db)` async helper function (NOT a fixture — called directly in tests): accepts an open connection, inserts a goal with `str(uuid.uuid4())` id, returns the id
- `create_task(db, goal_id)` async helper: accepts connection + goal_id, inserts a task row, returns task id

Create `tests/test_database.py`:
- Mark entire module with `pytestmark = pytest.mark.xfail(reason="runtime.database not yet implemented", strict=False)` OR use `pytest.importorskip("runtime.database")` at top
- Write async test functions: `test_open_write`, `test_wal_mode`, `test_busy_timeout`, `test_foreign_keys`, `test_init_schema_creates_all_tables`, `test_db_up_idempotent`, `test_db_reset`
- Each test body is a meaningful assertion stub — not `pass`, but `assert False, "not implemented"` so when xfail is removed they fail RED correctly
- `test_init_schema_creates_all_tables`: queries `sqlite_master` for all 7 table names after init_schema

Create `tests/test_models.py`:
- Import guard: `pytest.importorskip("runtime.models")` at module level
- Write sync test functions: `test_goal_model_roundtrip`, `test_task_rejects_invalid_status`, `test_all_models_construct`, `test_task_status_no_rejected_value`
- `test_task_status_no_rejected_value`: asserts `"rejected"` is not in `[s.value for s in TaskStatus]`
- `test_all_models_construct`: instantiates Goal, Task, TaskComment, TaskReview, AgentStatus, Document, ActivityLog with minimal required fields — all must succeed

Create `tests/test_state_machine.py`:
- Import guard: `pytest.importorskip("runtime.state_machine")` at module level
- Define `TRANSITION_CASES` list of `(from_state, to_state, should_succeed)` tuples matching RESEARCH.md Pattern 4 exactly:
  - Valid: TODO→IN_PROGRESS, IN_PROGRESS→PEER_REVIEW, PEER_REVIEW→REVIEW, PEER_REVIEW→IN_PROGRESS (rejection path), REVIEW→APPROVED
  - Invalid: TODO→APPROVED, TODO→PEER_REVIEW, APPROVED→TODO, IN_PROGRESS→APPROVED, REVIEW→IN_PROGRESS, IN_PROGRESS→TODO (regression guard)
- Single `@pytest.mark.parametrize("from_state,to_state,should_succeed", TRANSITION_CASES)` test function `test_transition`
- `test_error_message`: verifies InvalidTransitionError message contains both state strings

IMPORTANT: The conftest.py `db` fixture MUST handle the case where `runtime.database` does not exist yet. Use:
```python
try:
    from runtime.database import DatabaseManager
    _has_runtime = True
except ImportError:
    _has_runtime = False
```
And in the fixture body: `pytest.skip("runtime.database not yet available")` if `not _has_runtime`.

This ensures `pytest tests/` runs clean (no ImportError explosions) even before Plans 02-04 are executed.
  </action>
  <verify>
    <automated>cd "C:/Projects/Agent Creation" && pip install -e ".[dev]" -q && pytest tests/ --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects all test files without ImportError. Tests show as xfail/skip (not ERROR). `tests/__init__.py`, `tests/conftest.py`, `tests/test_database.py`, `tests/test_models.py`, `tests/test_state_machine.py` all exist.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `python -c "import runtime; print('OK')"` — package importable
2. `python -c "from pathlib import Path; sql = Path('runtime/schema.sql').read_text(); assert 'CREATE TABLE IF NOT EXISTS goals' in sql; assert 'CREATE TABLE IF NOT EXISTS activity_log' in sql; print('schema OK')"` — schema has all tables
3. `pytest tests/ --collect-only -q` — all tests collected, no ERROR items, xfail/skip acceptable
4. `pytest tests/ -q` — suite runs without crashing (all xfail/skip, 0 failures)
</verification>

<success_criteria>
- runtime/__init__.py exists
- runtime/schema.sql has all 7 tables + WAL pragma preamble + 3 indexes
- tests/ package exists with conftest.py + 3 test files
- `pytest tests/ --collect-only` returns 0 errors
- `pytest tests/ -q` exits 0 (xfail is not failure)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-runtime-database-state-machine/01-01-SUMMARY.md` using the summary template.
</output>
