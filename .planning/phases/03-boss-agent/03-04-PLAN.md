---
phase: 03-boss-agent
plan: "04"
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - tests/test_boss.py
  - tests/test_boss_cli.py
  - .planning/phases/03-boss-agent/03-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - boss-agent

must_haves:
  truths:
    - "pytest --cov=runtime --cov-fail-under=80 exits 0 (all tests GREEN, coverage >= 80%)"
    - "All 18 tests in test_boss.py GREEN (0 xfail)"
    - "All 7 tests in test_boss_cli.py GREEN (0 xfail)"
    - "No test regressions — all Phase 1 + 2 tests still GREEN"
    - "03-VALIDATION.md has nyquist_compliant: true and full per-task test map"
    - "STATE.md and ROADMAP.md reflect Phase 3 COMPLETE"
  artifacts:
    - path: "tests/test_boss.py"
      provides: "18 GREEN boss unit tests"
    - path: "tests/test_boss_cli.py"
      provides: "7 GREEN boss CLI integration tests"
    - path: ".planning/phases/03-boss-agent/03-VALIDATION.md"
      provides: "Completed validation contract with nyquist_compliant: true"
  key_links:
    - from: "pytest --cov=runtime"
      to: "runtime/boss.py"
      via: "coverage measurement"
      pattern: "boss\\.py.*\\d+%"
---

<objective>
Wave 4: Coverage gate + documentation finalization for Phase 3.

Purpose: Final gate plan to verify all tests are GREEN and coverage threshold is met before Phase 3 is declared complete. Also updates the phase validation contract (03-VALIDATION.md) and project state documents (STATE.md, ROADMAP.md). Any gaps found trigger fixes before sign-off.

Output:
- Full test suite GREEN (25 boss tests + all prior phase tests)
- Coverage >= 80% for runtime/ package
- 03-VALIDATION.md updated from draft to complete
- STATE.md and ROADMAP.md updated for Phase 3 COMPLETE
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-boss-agent/03-02-SUMMARY.md
@.planning/phases/03-boss-agent/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any coverage gaps</name>
  <files>tests/test_boss.py, tests/test_boss_cli.py</files>
  <action>
Run the full test suite with coverage:

```bash
pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80
```

**If coverage >= 80% and all tests GREEN:** Proceed to Task 2 immediately.

**If coverage is below 80%:** Identify uncovered lines in `runtime/boss.py` from the coverage report. Common gaps:

1. **Exception paths in LLM calls** — `_decompose_goal` except block, `_post_unblocking_hint` except block, `_check_goal_completion` except block. Add tests that patch `boss._llm.messages.parse` to raise an exception and verify fallback behavior.

2. **opus stays at opus escalation path** — If `test_stuck_task_escalates_model_tier_sonnet_to_opus` doesn't cover the case where `model_tier='opus'`, add a quick test asserting opus stays opus.

3. **_resolve_reviewer_agents no-agent-found path** — Warning log path when a role has no matching agent in agent_status. Add test with empty agent_status.

4. **_gap_fill_and_completion_check no-active-goal path** — Early return when goals table is empty. Add test.

5. **_gap_fill_and_completion_check with active tasks path** — The branch where `cnt > 0` skips gap fill. Add test.

For each gap, write the minimal test in the appropriate test file. Follow the established async patterns. Run the full suite again after each fix.

**Target: 0 uncovered lines in boss.py for the happy path. Allow coverage failure on LLM call interiors only if marked # pragma: no cover.**

If a line is truly untestable without a live LLM (e.g., specific Anthropic SDK error types), add `# pragma: no cover` to that line and document it in the task output.
  </action>
  <verify>
    <automated>pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80 2>&amp;1 | tail -20</automated>
  </verify>
  <done>pytest exits 0. All tests GREEN. Coverage >= 80% for runtime/ package. No xfail tests remain in test_boss.py or test_boss_cli.py.</done>
</task>

<task type="auto">
  <name>Task 2: Update 03-VALIDATION.md, STATE.md, and ROADMAP.md</name>
  <files>.planning/phases/03-boss-agent/03-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**Update .planning/phases/03-boss-agent/03-VALIDATION.md** — replace the draft template with the completed validation contract:

```markdown
---
phase: 3
slug: boss-agent
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-02
completed: [TODAY'S DATE]
---

# Phase 3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.0+ with pytest-asyncio 0.24+ |
| **Config file** | pyproject.toml — `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` |
| **Quick run command** | `pytest tests/test_boss.py tests/test_boss_cli.py -x --no-cov` |
| **Full suite command** | `pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_boss.py tests/test_boss_cli.py -x --no-cov`
- **After every plan wave:** Run `pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task | Plan | Wave | Behavior | Test Type | Command | Status |
|------|------|------|----------|-----------|---------|--------|
| Schema + tabulate | 03-00 | 0 | reviewer_roles column in schema | unit | `python -c "import sqlite3..."` | ✅ green |
| Boss test stubs | 03-00 | 0 | 25 stubs collected | collect | `pytest --collect-only` | ✅ green |
| BossAgent structure | 03-01 | 1 | is BaseAgent, has _llm, counter=0 | unit | `pytest tests/test_boss.py -k structure` | ✅ green |
| Peer review promotion | 03-01 | 1 | peer_review → review when all approved | unit | `pytest tests/test_boss.py -k promote` | ✅ green |
| Rejection handling | 03-01 | 1 | any rejection → in-progress, reset reviews | unit | `pytest tests/test_boss.py -k rejection` | ✅ green |
| Goal decomposition | 03-01 | 1 | LLM → 3-5 tasks inserted | unit (mocked) | `pytest tests/test_boss.py -k decompose` | ✅ green |
| Reviewer roles | 03-01 | 1 | reviewer_roles stored as JSON | unit | `pytest tests/test_boss.py -k reviewer_roles` | ✅ green |
| Re-review upsert | 03-01 | 1 | INSERT OR REPLACE on rejection | unit | `pytest tests/test_boss.py -k re_review` | ✅ green |
| Stuck detection haiku→sonnet | 03-02 | 2 | model_tier escalates | unit | `pytest tests/test_boss.py -k haiku_to_sonnet` | ✅ green |
| Stuck detection sonnet→opus | 03-02 | 2 | model_tier escalates | unit | `pytest tests/test_boss.py -k sonnet_to_opus` | ✅ green |
| stuck_since set | 03-02 | 2 | NULL → timestamp on first escalation | unit | `pytest tests/test_boss.py -k stuck_since` | ✅ green |
| Second intervention | 03-02 | 2 | LLM hint posted as task_comment | unit (mocked) | `pytest tests/test_boss.py -k second_intervention` | ✅ green |
| Escalation activity log | 03-02 | 2 | action=task_escalated with JSON details | unit | `pytest tests/test_boss.py -k activity_log` | ✅ green |
| Gap-fill every 3 heartbeats | 03-02 | 2 | called on 3,6,9 not 1,2 | unit | `pytest tests/test_boss.py -k gap_fill` | ✅ green |
| Goal completion | 03-02 | 2 | LLM True → goal.status=completed | unit (mocked) | `pytest tests/test_boss.py -k goal_completion` | ✅ green |
| CLI goal set | 03-03 | 3 | inserts goal, triggers decompose | integration | `pytest tests/test_boss_cli.py -k goal_set` | ✅ green |
| CLI tasks list table | 03-03 | 3 | tabulate table with correct columns | integration | `pytest tests/test_boss_cli.py -k table_output` | ✅ green |
| CLI tasks list filter | 03-03 | 3 | --status filters correctly | integration | `pytest tests/test_boss_cli.py -k status_filter` | ✅ green |
| CLI tasks list JSON | 03-03 | 3 | --json outputs valid JSON array | integration | `pytest tests/test_boss_cli.py -k json_output` | ✅ green |
| CLI agents status | 03-03 | 3 | table with agent rows | integration | `pytest tests/test_boss_cli.py -k agents_status` | ✅ green |
| CLI approve review | 03-03 | 3 | exit 0, status=approved in DB | integration | `pytest tests/test_boss_cli.py -k approve_review` | ✅ green |
| CLI approve wrong state | 03-03 | 3 | exit 1 with error message | integration | `pytest tests/test_boss_cli.py -k wrong_state` | ✅ green |

---

## Wave 0 Requirements

All Wave 0 gaps were addressed in plan 03-00:
- [x] `tests/test_boss.py` — 18 stubs created
- [x] `tests/test_boss_cli.py` — 7 stubs created
- [x] `runtime/schema.sql` — reviewer_roles TEXT column added
- [x] `pyproject.toml` — tabulate>=0.9.0 added

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Live LLM decomposition quality | Requires ANTHROPIC_API_KEY and real API call | `cluster goal set "Write a Python utility library"` with real key; verify 3-5 meaningful tasks created |
| Live stuck detection timing | Requires 30+ min wait | Run cluster with short interval, inject old timestamp, verify escalation fires |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete [TODAY'S DATE]
```

**Update .planning/STATE.md** — add a new session log entry at the top of the Session Log section and update Current Position:

Add to Session Log (prepend before existing 2026-03-02 entries):
```
### [TODAY'S DATE] — Phase 3 executed (BossAgent — all 4 plans complete)
- Stopped at: Completed 03-04-PLAN.md
- Last commit: [git log --oneline -1]
- Key decisions: BossAgent subclasses BaseAgent overriding do_peer_reviews/do_own_tasks; reviewer_roles stored as JSON TEXT on tasks table; tabulate used for CLI table output; AsyncAnthropic.messages.parse() for structured LLM output; cluster goal set archives old active goal before inserting new; INSERT OR REPLACE handles task_reviews UNIQUE constraint on rejection path
```

Update Current Position section:
```
## Current Position

- Phase 3 of 7: Boss Agent — COMPLETE
- Current Plan: 04 of 04 complete (all waves: stubs + BossAgent core + stuck detection/escalation + CLI commands)
- Status: Plan 03-04 complete (25 boss tests GREEN, full suite >= 80% coverage); Phase 3 DONE
- Next: Phase 4 — Worker Agents (task execution, peer review execution, role-based system prompts)
```

**Update .planning/ROADMAP.md** — add status to Phase 3:

Find the Phase 3 section and add status line after the Success Criteria block:
```
**Status: COMPLETE** — 4/4 plans executed, 25 GREEN boss tests, coverage >= 80%

Plans:
- [x] 03-00-PLAN.md — TDD RED stubs: 18 boss tests + 7 CLI stubs; schema + tabulate
- [x] 03-01-PLAN.md — BossAgent core: peer review promotion + goal decomposition (10 GREEN)
- [x] 03-02-PLAN.md — Stuck detection + escalation + gap-fill cron (18 GREEN)
- [x] 03-03-PLAN.md — CLI commands: goal set, tasks list, agents status, approve (25 GREEN)
- [x] 03-04-PLAN.md — Coverage gate + documentation finalization
```
  </action>
  <verify>
    <automated>pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80 2>&amp;1 | grep -E "PASSED|FAILED|ERROR|coverage" | tail -10</automated>
  </verify>
  <done>03-VALIDATION.md has nyquist_compliant: true and status: complete. STATE.md has Phase 3 session log entry. ROADMAP.md has Phase 3 status: COMPLETE with plan checklist. Full test suite GREEN with >= 80% coverage.</done>
</task>

</tasks>

<verification>
Final verification for Phase 3:

```bash
# Complete test suite — ALL green, coverage >= 80%
pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80

# Boss tests specifically
pytest tests/test_boss.py tests/test_boss_cli.py -v --no-cov

# CLI smoke test (entry points accessible)
python -m runtime.cli cluster goal --help
python -m runtime.cli cluster tasks --help
python -m runtime.cli cluster agents --help
python -m runtime.cli cluster approve --help

# Validation doc is complete
python -c "
import pathlib
v = pathlib.Path('.planning/phases/03-boss-agent/03-VALIDATION.md').read_text()
assert 'nyquist_compliant: true' in v
assert 'status: complete' in v
print('Validation doc OK')
"
```
</verification>

<success_criteria>
- pytest --cov=runtime --cov-fail-under=80 exits 0
- 18 tests GREEN in tests/test_boss.py (0 xfail)
- 7 tests GREEN in tests/test_boss_cli.py (0 xfail)
- All Phase 1 + Phase 2 tests still GREEN (no regressions)
- 03-VALIDATION.md: nyquist_compliant: true, status: complete
- STATE.md: Phase 3 session log entry added, Current Position updated to Phase 4
- ROADMAP.md: Phase 3 marked COMPLETE with plan checklist
</success_criteria>

<output>
After completion, create `.planning/phases/03-boss-agent/03-04-SUMMARY.md` with:
- Final test counts (18 boss unit + 7 CLI = 25 total)
- Final coverage percentage for runtime/boss.py
- Any lines marked pragma: no cover and why
- Phase 3 completion confirmation
</output>
