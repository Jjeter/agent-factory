---
phase: 02-agent-heartbeat-framework
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_config.py
  - tests/test_notifier.py
  - tests/test_heartbeat.py
autonomous: true
requirements:
  - HB-01
  - HB-02
  - HB-03
  - HB-04
  - HB-05
  - HB-06
  - HB-07
  - HB-08
  - HB-09
  - HB-10
  - HB-11
  - HB-12
  - HB-13

must_haves:
  truths:
    - "All 13 requirement test functions exist and are importable (even if skipped/xfail)"
    - "pytest can collect all three test files without ImportError"
    - "runtime/state/ is already present in .gitignore"
  artifacts:
    - path: "tests/test_config.py"
      provides: "Stub tests for HB-01 (interval ge constraint) and HB-02 (YAML load)"
    - path: "tests/test_notifier.py"
      provides: "Stub test for HB-03 (StdoutNotifier protocol)"
    - path: "tests/test_heartbeat.py"
      provides: "Stub tests for HB-04 through HB-13 (heartbeat loop behaviors)"
  key_links:
    - from: "tests/test_heartbeat.py"
      to: "runtime/heartbeat.py"
      via: "lazy import inside each test (skips when module not yet implemented)"
      pattern: "pytest.importorskip|try.*import"
    - from: "tests/test_config.py"
      to: "runtime/config.py"
      via: "lazy import inside each test"
      pattern: "pytest.importorskip|try.*import"
---

<objective>
Create all test stub files for Phase 2 before any implementation exists (TDD: RED phase).
These stubs define every expected behavior and allow `pytest --collect-only` to succeed now.
Each test uses `pytest.importorskip` so it skips cleanly until the implementation lands in
Wave 1 and Wave 2 — no ImportError, no blocking the test runner.

Purpose: Establish the full test contract upfront. Wave 1 and Wave 2 implementations are
written against these test expectations, not the other way around.

Output: Three test files covering all HB-01 through HB-13 requirements.
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-heartbeat-framework/02-CONTEXT.md
@.planning/phases/02-agent-heartbeat-framework/02-VALIDATION.md
@tests/conftest.py
@pyproject.toml

<interfaces>
<!-- Existing patterns from conftest.py to follow -->

From tests/conftest.py:
```python
# Lazy import guard pattern used in conftest:
try:
    from runtime.database import DatabaseManager
    _has_runtime = True
except ImportError:
    _has_runtime = False

# Per-test function-scoped async fixture (asyncio_mode=auto in pyproject.toml)
@pytest.fixture
async def db():
    if not _has_runtime:
        pytest.skip("runtime.database not yet available")
    ...

# pyproject.toml: asyncio_mode = "auto" — all async test functions auto-detected
```

Phase 2 modules NOT YET IMPLEMENTED (use pytest.importorskip):
- runtime/config.py  → AgentConfig, load_agent_config
- runtime/notifier.py → Notifier (Protocol), StdoutNotifier
- runtime/heartbeat.py → BaseAgent

From runtime/models.py (already exists — safe to import directly):
```python
from runtime.models import AgentStatus, AgentStatusEnum, _now_iso
```

From runtime/database.py (already exists — safe to import directly):
```python
from runtime.database import DatabaseManager
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_config.py — stubs for HB-01 and HB-02</name>
  <files>tests/test_config.py</files>
  <action>
Create `tests/test_config.py` with two test stub functions. Use `pytest.importorskip` at
the top of each test body (NOT at module level) so pytest can collect the file before the
module exists. Both tests should be skippable stubs that mark themselves as expected-to-fail
when the implementation is absent.

Pattern to follow: use `pytest.importorskip("runtime.config")` inside each test function.
This skips the test (not errors) when the module is missing.

Required test functions (exact names, as they appear in VALIDATION.md):

**test_interval_ge_constraint** (covers HB-01):
  Behavior: `AgentConfig` with `interval_seconds=0.005` (below 0.01) raises a
  `pydantic.ValidationError`. `AgentConfig` with `interval_seconds=0.01` succeeds.
  Import: `from runtime.config import AgentConfig`
  Implementation stub body:
  - Skip if module absent via `pytest.importorskip`
  - `import pydantic` — import ValidationError
  - Assert `AgentConfig(agent_id="a", agent_role="worker", db_path="/tmp/x.db",
    interval_seconds=0.005)` raises `pydantic.ValidationError`
  - Assert `AgentConfig(agent_id="a", agent_role="worker", db_path="/tmp/x.db",
    interval_seconds=0.01)` does NOT raise

**test_load_agent_config** (covers HB-02):
  Behavior: `load_agent_config(path)` reads a YAML file and returns an `AgentConfig`.
  Import: `from runtime.config import AgentConfig, load_agent_config`
  Implementation stub body:
  - Skip if module absent via `pytest.importorskip`
  - Write a minimal YAML file to `tmp_path / "agent.yaml"` with content:
    `agent_id: test-agent\nagent_role: worker\ndb_path: /tmp/test.db\n`
  - Call `load_agent_config(tmp_path / "agent.yaml")`
  - Assert result is an `AgentConfig` instance
  - Assert `result.agent_id == "test-agent"`
  - Assert `result.agent_role == "worker"`

File header: include module docstring explaining this is the TDD stub file for Phase 2 config.
Standard imports at top: `import pytest` (other imports lazy inside each test).
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>
pytest can collect tests/test_config.py without ImportError.
Two test functions visible: test_interval_ge_constraint, test_load_agent_config.
Both skip (not error) since runtime/config.py does not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_notifier.py — stub for HB-03</name>
  <files>tests/test_notifier.py</files>
  <action>
Create `tests/test_notifier.py` with one test stub function covering HB-03.

**test_stdout_notifier** (covers HB-03):
  Behavior: `StdoutNotifier` satisfies the `Notifier` protocol — all three methods callable
  and return coroutines (are async functions), and they actually await without error.
  Import: `from runtime.notifier import Notifier, StdoutNotifier`
  Implementation stub body:
  - Skip if module absent: `pytest.importorskip("runtime.notifier")`
  - Instantiate `notifier = StdoutNotifier()`
  - Assert `notifier` is an instance where all three methods exist:
    `notify_review_ready`, `notify_escalation`, `notify_cluster_ready`
  - `await notifier.notify_review_ready(task_id="t-1", task_title="Test task")` — no exception
  - `await notifier.notify_escalation(task_id="t-1", reason="2 rejections")` — no exception
  - `await notifier.notify_cluster_ready(cluster_name="demo", path="/tmp/demo")` — no exception
  - Use `capsys` fixture to capture stdout and assert each call printed something (non-empty
    output from `print()`). This verifies the implementation is not a silent no-op.

Note: The test function must be `async def` so pytest-asyncio handles it (asyncio_mode=auto).

File header: module docstring explaining TDD stub file for Phase 2 notifier.
Standard imports: `import pytest` at module level only.
  </action>
  <verify>
    <automated>python -m pytest tests/test_notifier.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>
pytest can collect tests/test_notifier.py without ImportError.
One test function visible: test_stdout_notifier.
Test skips (not errors) since runtime/notifier.py does not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_heartbeat.py — stubs for HB-04 through HB-13</name>
  <files>tests/test_heartbeat.py</files>
  <action>
Create `tests/test_heartbeat.py` with 10 test stub functions covering HB-04 through HB-13.
All test functions should use `pytest.importorskip("runtime.heartbeat")` at the start of
their body (NOT module level) to skip cleanly before the module exists.

All async test functions use `async def` — pytest-asyncio handles them automatically.

The file needs a `FixedTickAgent` helper class defined INSIDE each relevant test (or as a
module-level class guarded with a module-level import check). To avoid ImportError at
collection time, define `FixedTickAgent` inside each test function that needs it, after the
importorskip call, OR use a module-level try/except that sets a sentinel.

Recommended approach: module-level guard with sentinel:
```python
try:
    from runtime.heartbeat import BaseAgent
    from runtime.config import AgentConfig
    _has_heartbeat = True
except ImportError:
    _has_heartbeat = False
    BaseAgent = object  # placeholder so class body parses
    AgentConfig = None
```

Then each test starts with: `if not _has_heartbeat: pytest.skip("runtime.heartbeat not yet available")`

Required test functions (exact names):

**test_stagger_delay** (HB-04):
  Behavior: An agent with `stagger_offset_seconds=0.1` takes at least 0.1 seconds before
  its first tick executes.
  Approach: Subclass BaseAgent, record timestamp in `_tick`, run with
  `stagger_offset_seconds=0.1`, verify elapsed >= 0.09s (allow small tolerance).
  Uses real SQLite `tmp_path` db.

**test_status_upsert** (HB-05):
  Behavior: After one tick, `agent_status` table has exactly one row for the agent's id.
  Running two ticks still yields one row (UPSERT, not INSERT).
  Uses real SQLite `tmp_path` db.
  Uses `FixedTickAgent` (stops after N ticks).

**test_status_transitions** (HB-06):
  Behavior: During a tick, `agent_status.status == 'working'`. After tick completes,
  status == 'idle'.
  Approach: Subclass BaseAgent, override `do_own_tasks` to snapshot the DB status mid-tick.
  After `start()` returns, read final status from DB and assert 'idle'.
  Uses real SQLite `tmp_path` db.

**test_state_file_written** (HB-07):
  Behavior: After one tick, `runtime/state/<agent-id>.json` exists, contains valid JSON
  with keys `last_heartbeat` and `current_task_id`.
  Uses real SQLite `tmp_path` db and `tmp_path` for state dir (override STATE_DIR in test).
  Approach: Patch `runtime.heartbeat.STATE_DIR` to point to `tmp_path / "state"` to avoid
  writing to the real runtime/state directory during tests.

**test_state_file_corrupt** (HB-08):
  Behavior: If `<agent-id>.json` contains invalid JSON at startup, agent logs a warning and
  treats it as a fresh start (does not raise, runs normally).
  Approach: Pre-write corrupt JSON to state path, then start agent for 1 tick — no exception.
  Use `caplog` fixture to assert a WARNING message was logged containing "corrupt" or "missing".

**test_cancelled_error_propagates** (HB-09):
  Behavior: If `asyncio.create_task(agent.start())` is cancelled, `CancelledError` propagates
  out of `start()` — it is not swallowed.
  Approach: Create task, let it run one tick, cancel it, await it, assert `asyncio.CancelledError`
  is raised (not silently suppressed).

**test_stop_event_graceful** (HB-10):
  Behavior: Setting the agent's `_stop_event` terminates the loop within the next sleep
  interval (does not hang).
  Approach: Start agent as task, set `_stop_event` after a short delay, await with timeout.
  Assert completes within `interval_seconds * 2 + 0.5` seconds.
  Uses `asyncio.wait_for` in the test to catch hangs.

**test_two_agents_no_db_collision** (HB-11):
  Behavior: Two `FixedTickAgent` instances (3 ticks each) run concurrently via
  `asyncio.gather()` on a real SQLite tmp file without raising `OperationalError`.
  Stagger offsets: agent-1 = 0.0s, agent-2 = 0.025s. Interval: 0.05s.
  After gather completes, assert `agent_status` has exactly 2 rows (one per agent).
  This is an integration test that surfaces `database is locked` errors.

**test_hook_order** (HB-12):
  Behavior: Within a single tick, `do_peer_reviews()` is called before `do_own_tasks()`.
  Approach: Subclass BaseAgent, record call order in a list during each override.
  After one tick, assert `["do_peer_reviews", "do_own_tasks"]` order.

**test_jitter_clamped** (HB-13):
  Behavior: With `interval_seconds=0.01` (minimum) and jitter ±30s, `asyncio.sleep` is
  never called with a negative value — no `ValueError` raised.
  Approach: Patch `asyncio.sleep` (or use `unittest.mock.patch`) to record call args.
  Run 3 ticks. Assert all recorded sleep durations >= 0.0.
  Alternatively: just run 5 ticks with interval=0.01 and assert no ValueError propagates.

**FixedTickAgent helper** — define at module level (after the import guard):
```python
if _has_heartbeat:
    class FixedTickAgent(BaseAgent):
        def __init__(self, config, ticks: int, db_path_override=None):
            super().__init__(config)
            self._remaining = ticks

        async def _tick(self):
            await super()._tick()
            self._remaining -= 1
            if self._remaining <= 0:
                self._stop_event.set()
else:
    FixedTickAgent = None
```

All tests that use `tmp_path` for DB must initialize schema first:
```python
from runtime.database import DatabaseManager
mgr = DatabaseManager(db_file)
await mgr.up()
```

The `AgentConfig` constructor signature for all tests:
```python
AgentConfig(
    agent_id="agent-1",
    agent_role="worker",
    db_path=str(db_file),
    interval_seconds=0.05,
    stagger_offset_seconds=0.0,
)
```

CRITICAL: Do NOT import `runtime.heartbeat` at module level — only inside tests or behind
the `_has_heartbeat` sentinel. This keeps pytest collection clean before implementation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_heartbeat.py --collect-only -q 2>&1 | head -30</automated>
  </verify>
  <done>
pytest can collect tests/test_heartbeat.py without ImportError.
All 10 test functions visible in collection output.
All tests skip (not error) since runtime/heartbeat.py does not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify .gitignore and run full collection check</name>
  <files>.gitignore</files>
  <action>
1. Read `.gitignore` (already exists per git status). Verify `runtime/state/` is present.
   The file already contains this entry per the git status showing `.gitignore` as untracked.
   If `runtime/state/` is missing from `.gitignore`, add it. If already present, no change.

2. After all three test files are created, run the full collection check to confirm all
   13 tests are discoverable and no file causes an ImportError:
   `pytest tests/test_config.py tests/test_notifier.py tests/test_heartbeat.py --collect-only -q`

   Expected output: 13 test items collected (2 + 1 + 10), 0 errors.

3. Also verify the existing Phase 1 test suite still passes:
   `pytest tests/test_database.py tests/test_models.py tests/test_state_machine.py tests/test_cli.py -x -q`

   This confirms the new stub files don't break anything.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py tests/test_notifier.py tests/test_heartbeat.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>
`runtime/state/` present in .gitignore.
All 13 Phase 2 test stubs collected without error.
Phase 1 tests unaffected (all still pass).
  </done>
</task>

</tasks>

<verification>
Run after all tasks complete:
```
pytest tests/test_config.py tests/test_notifier.py tests/test_heartbeat.py --collect-only -q
```
Expected: 13 items collected, 0 errors, 0 failures.

Then run full suite to confirm no Phase 1 regressions:
```
pytest --cov=runtime --cov-report=term-missing -q
```
Expected: Phase 1 tests pass, Phase 2 tests skip (no xfail errors).
</verification>

<success_criteria>
- 13 test stub functions exist across 3 files, collectible by pytest
- All 13 skip cleanly (no ImportError) before implementation modules exist
- .gitignore contains runtime/state/ entry
- Phase 1 test suite unaffected (still GREEN)
- HB-01 through HB-13 all have a named test stub
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-heartbeat-framework/02-00-SUMMARY.md`
</output>
