---
phase: 07-hardening-v0-1-0-release
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements:
  - COV-01
  - DOCS-01
  - PKG-01

must_haves:
  truths:
    - "CHANGELOG.md exists at project root with a v0.1.0 section"
    - "Full pytest suite passes with 80%+ coverage"
    - "User has registered Trusted Publisher on both TestPyPI and PyPI before tagging"
    - "GitHub release v0.1.0 has .whl and .tar.gz attached after tag push"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Human-readable v0.1.0 release notes summarizing all 6 prior phases"
      contains: "## [0.1.0]"
    - path: ".github/workflows/publish.yml"
      provides: "Publish pipeline (created in 07-01, verified here)"
  key_links:
    - from: "git tag v0.1.0"
      to: ".github/workflows/publish.yml"
      via: "on: push: tags: [v*] trigger"
      pattern: "tags.*v"
---

<objective>
Final wave: write CHANGELOG.md, run the coverage gate, and walk the user through the one-time
manual PyPI Trusted Publisher registration before pushing the v0.1.0 tag.

Purpose: Complete all release documentation requirements and gate the actual PyPI publish behind
human verification of the Trusted Publisher setup — this is the only step Claude cannot automate.

Output:
- CHANGELOG.md (human-readable, Keep a Changelog format)
- Coverage gate verified (pytest --cov-fail-under=80 passes)
- Checkpoint: user registers Trusted Publisher on TestPyPI + PyPI, then pushes the v0.1.0 tag
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-hardening-v0-1-0-release/07-CONTEXT.md
@.planning/phases/07-hardening-v0-1-0-release/07-01-SUMMARY.md
@.planning/phases/07-hardening-v0-1-0-release/07-02-SUMMARY.md
@.planning/phases/07-hardening-v0-1-0-release/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md and run final coverage gate</name>
  <files>CHANGELOG.md</files>
  <action>
**Part A — Write CHANGELOG.md at project root.**

Follow Keep a Changelog conventions (https://keepachangelog.com/en/1.0.0/).
This is human-written prose summarizing capabilities — NOT a raw commit dump.

Structure:
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [0.1.0] - 2026-03-09

### Added

- **Factory cluster**: `agent-factory create "<goal>"` generates a fully self-contained AI agent
  cluster artifact (docker-compose.yml, config YAML, runtime copy, seeded database, launch.sh)
  from a natural-language goal description.

- **Boss/worker heartbeat loop**: Generic async `BaseAgent` with configurable interval, jitter,
  and stagger offsets. `BossAgent` coordinates task lifecycle: decomposes goals into tasks,
  promotes tasks through peer review, escalates stuck tasks, detects AWOL agents, and fills
  gaps when no work is in flight. `WorkerAgent` executes tasks with role-based system prompts,
  submits outputs for peer review, and performs substantive peer reviews.

- **Task state machine**: Strict five-state machine (todo → in-progress → peer_review → review
  → approved) with typed `InvalidTransitionError`. Only the boss may promote to review; only the
  user may approve.

- **Model tier escalation**: Tasks escalate from haiku → sonnet → opus automatically after
  repeated peer review rejections.

- **Demo cluster**: Pre-built `clusters/demo-date-arithmetic/` artifact demonstrating a
  date arithmetic Python utility library cluster, ready to run with `./launch.sh`.

- **CLI**: `agent-factory` commands (create, list, status, approve, logs, demo) and
  `cluster` commands (goal set, tasks list, agents status, approve, logs).

- **Security hardening**: Tool allowlist enforcement (WorkerAgent rejects disallowed LLM tool
  calls), cross-cluster database isolation (path-based SQLite), AWOL agent detection with
  `activity_log` entries, and crash recovery (agent resumes prior in-progress task on restart).

- **CI**: GitHub Actions smoke test (Docker Compose cluster startup), secrets scan
  (truffleHog on every push and PR), and PyPI publish pipeline (TestPyPI gate → PyPI →
  GitHub Release) triggered on `v*` tag push with OIDC trusted publishing.
```

**Part B — Run final coverage gate:**

```bash
cd /c/Projects/Agent\ Creation && python -m pytest --tb=short 2>&1 | tail -30
```

The suite must show all tests GREEN and coverage >= 80%. The `--cov-fail-under=80` flag in
pyproject.toml addopts will cause pytest to exit non-zero if coverage drops below 80%.

If coverage is below 80% (unlikely given current 85.78% baseline), identify which new files
are untested and add targeted tests before proceeding. Do not reduce the threshold.
  </action>
  <verify>
    <automated>cd /c/Projects/Agent\ Creation && python -m pytest --tb=short 2>&1 | grep -E "passed|failed|error|coverage" | tail -10</automated>
  </verify>
  <done>
- CHANGELOG.md exists at project root with ## [0.1.0] section
- pytest exits 0, all tests GREEN, coverage >= 80%
- No FAILED or ERROR lines in pytest output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PyPI Trusted Publisher setup + v0.1.0 tag push</name>
  <action>
Human-required steps to register Trusted Publisher on TestPyPI and PyPI, create GitHub
Environments, push the v0.1.0 tag, and verify the publish workflow completes successfully.
These steps cannot be automated — they require browser interactions with external registries
and a git tag push to trigger the workflow.
  </action>
  <what-built>
Phase 7 implementation is complete:
- tests/test_security.py: 2 GREEN security tests (tool allowlist, cross-cluster DB isolation)
- runtime/worker.py: tool allowlist enforcement in _execute_task()
- runtime/boss.py: AWOL detection (_check_awol_agents) with dedup + activity_log entries
- runtime/heartbeat.py + runtime/worker.py: crash recovery (_resumed_task_id path)
- .github/workflows/security.yml: truffleHog secrets scan CI gate
- .github/workflows/publish.yml: TestPyPI → PyPI → GitHub Release pipeline
- CHANGELOG.md: v0.1.0 release notes

All 130+ tests GREEN, coverage >= 80%.
  </what-built>
  <how-to-verify>
**Step 1 — Verify the full test suite:**
```
cd "C:\Projects\Agent Creation"
python -m pytest --tb=short
```
Expected: All tests GREEN, coverage >= 80%.

**Step 2 — Register Trusted Publishers on PyPI (one-time, required before tagging):**

a. Log in to https://test.pypi.org
   - Navigate to your account -> Publishing
   - Add a new Trusted Publisher:
     - Package name: `agent-factory`
     - Owner: your GitHub username
     - Repository: `Agent Creation` (the repo name)
     - Workflow name: `publish.yml`
     - Environment name: `testpypi`

b. Log in to https://pypi.org
   - Navigate to your account -> Publishing
   - Add a new Trusted Publisher with the same settings EXCEPT:
     - Environment name: `pypi`

**Step 3 — Create GitHub Environments in the repository settings:**
   - Go to the repository Settings -> Environments
   - Create environment named `testpypi`
   - Create environment named `pypi` (optionally add a required reviewer)

**Step 4 — Push the v0.1.0 tag:**
```
git tag v0.1.0
git push origin v0.1.0
```

**Step 5 — Monitor the publish workflow:**
   - Go to GitHub Actions -> Publish workflow
   - Watch build -> test-publish (TestPyPI) -> publish (PyPI) -> github-release
   - All 4 jobs should show green checkmarks

**Step 6 — Verify the release:**
```
pip install --index-url https://test.pypi.org/simple/ agent-factory
agent-factory --help
```
Expected: CLI help text displays correctly.

Check https://github.com/YOUR_USERNAME/YOUR_REPO/releases -- v0.1.0 release should exist with
.whl and .tar.gz attached as assets.
  </how-to-verify>
  <resume-signal>
Type "released" once all 6 steps above are complete and the GitHub release page shows the .whl and .tar.gz assets. Or describe any issues encountered.
  </resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:

- CHANGELOG.md at project root has ## [0.1.0] section
- GitHub release v0.1.0 exists with .whl + .tar.gz assets
- `pip install --index-url https://test.pypi.org/simple/ agent-factory && agent-factory --help` succeeds
- Secret Scanning CI workflow shows green on GitHub Actions
- Full test suite: all tests GREEN, 80%+ coverage
</verification>

<success_criteria>
- CHANGELOG.md written with human-readable v0.1.0 summary (Keep a Changelog format)
- pytest --cov-fail-under=80 exits 0 (all tests GREEN, coverage gate passes)
- User has pushed v0.1.0 tag and publish.yml workflow completed successfully on GitHub Actions
- GitHub release v0.1.0 exists with .whl and .tar.gz attached as assets
- Phase 7 complete — Agent Factory v0.1.0 is shipped
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-v0-1-0-release/07-04-SUMMARY.md`
</output>
