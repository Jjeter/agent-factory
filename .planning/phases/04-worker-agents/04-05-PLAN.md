---
phase: 04-worker-agents
plan: "05"
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - config/cluster.yaml
  - config/agents/researcher.yaml
  - config/agents/writer.yaml
  - config/agents/strategist.yaml
autonomous: true
requirements:
  - W-01
  - W-02
  - W-03
  - W-04
  - W-05
  - W-06
  - W-07
  - W-08
  - W-09
  - W-10
  - W-11
  - W-12
  - W-13
  - W-14
  - W-15
  - W-16
  - W-17
  - W-18

must_haves:
  truths:
    - "config/cluster.yaml exists with db_path, interval_seconds, jitter_seconds"
    - "config/agents/researcher.yaml, writer.yaml, strategist.yaml all exist with required fields"
    - "load_agent_config(role_path, cluster_config_path=cluster_path) returns valid AgentConfig for each role"
    - "All 18 test_worker.py tests are GREEN"
    - "pytest --cov=runtime --cov-fail-under=80 passes"
  artifacts:
    - path: "config/cluster.yaml"
      provides: "Shared base config for all agents"
      contains: "db_path"
    - path: "config/agents/researcher.yaml"
      provides: "Researcher role config with system_prompt and tool_allowlist"
      contains: "system_prompt"
    - path: "config/agents/writer.yaml"
      provides: "Writer role config with system_prompt and tool_allowlist"
      contains: "system_prompt"
    - path: "config/agents/strategist.yaml"
      provides: "Strategist role config with system_prompt and tool_allowlist"
      contains: "system_prompt"
  key_links:
    - from: "config/agents/researcher.yaml"
      to: "runtime/config.py load_agent_config()"
      via: "load_agent_config(role_path, cluster_config_path) merge"
      pattern: "tool_allowlist"
---

<objective>
Create role YAML config files (cluster.yaml + researcher/writer/strategist) and run the full coverage gate.

Purpose: The YAML configs are the user-facing configuration surface — they define each role's system prompt and tool allowlist. Coverage gate confirms all 18 W-requirements are GREEN before phase closes.
Output: config/cluster.yaml, config/agents/{researcher,writer,strategist}.yaml; full test suite GREEN at 80%+.
</objective>

<execution_context>
@C:/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-worker-agents/04-CONTEXT.md
@.planning/phases/04-worker-agents/04-RESEARCH.md
@.planning/phases/04-worker-agents/04-04-SUMMARY.md
@runtime/config.py

<interfaces>
YAML field requirements per CONTEXT.md decisions:

config/cluster.yaml fields (shared defaults):
- db_path: str  (e.g., "db/cluster.db")
- interval_seconds: float  (e.g., 600.0)
- jitter_seconds: float  (e.g., 30.0)

config/agents/<role>.yaml fields (role-specific):
- agent_id: str  (e.g., "researcher-01")
- agent_role: str  (e.g., "researcher")
- stagger_offset_seconds: float  (boss=0, researcher=150, writer=300, strategist=450)
- system_prompt: str  (multi-line markdown)
- tool_allowlist: list[str]  (empty list [] for Phase 4 — populated in Phase 5)

Stagger offsets for 4-agent cluster (boss offset 0, workers at 2.5 min intervals):
- researcher: 150.0 (2.5 min)
- writer: 300.0 (5.0 min)
- strategist: 450.0 (7.5 min)

System prompt guidance per role (Claude's discretion for exact wording — CONTEXT.md):
- researcher: findings in markdown, executive summary, key findings with evidence, sources cited, open questions
- writer: well-structured prose, clear narrative, audience-appropriate language, document-quality output
- strategist: recommendations format, options analysis, risk/benefit, actionable next steps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role YAML config files</name>
  <files>config/cluster.yaml, config/agents/researcher.yaml, config/agents/writer.yaml, config/agents/strategist.yaml</files>
  <action>
    Create the config/ directory structure if it does not exist.
    Create config/cluster.yaml:
    ```yaml
    # Shared base configuration for all agents in this cluster.
    # Role-specific YAML files override/extend these defaults.
    db_path: db/cluster.db
    interval_seconds: 600.0
    jitter_seconds: 30.0
    ```

    Create config/agents/researcher.yaml:
    ```yaml
    # Researcher agent — discovers information, synthesizes findings.
    agent_id: researcher-01
    agent_role: researcher
    stagger_offset_seconds: 150.0
    system_prompt: |
      You are a researcher agent. Produce well-structured findings in markdown.
      Include the following sections:
      - **Executive Summary**: 2-3 sentences summarizing the key finding.
      - **Key Findings**: Bullet points with supporting evidence or reasoning.
      - **Sources / Reasoning**: Cite specific sources or describe your reasoning chain.
      - **Open Questions**: Any unresolved issues or areas requiring further investigation.
      Be concise but thorough. Write for a technical audience. Do not pad output.
    tool_allowlist: []  # No tools in Phase 4; populated in Phase 5
    ```

    Create config/agents/writer.yaml:
    ```yaml
    # Writer agent — produces polished prose documents from research or strategy inputs.
    agent_id: writer-01
    agent_role: writer
    stagger_offset_seconds: 300.0
    system_prompt: |
      You are a writer agent. Produce clear, well-structured documents in markdown.
      Follow these guidelines:
      - Write for the stated audience; adjust technical depth accordingly.
      - Use headings, bullet points, and tables where they aid clarity.
      - Ensure a clear narrative arc: introduction, body, conclusion.
      - Prefer concrete, specific language over vague generalisations.
      - Proofread for grammar and consistency before finalising.
    tool_allowlist: []  # No tools in Phase 4; populated in Phase 5
    ```

    Create config/agents/strategist.yaml:
    ```yaml
    # Strategist agent — produces recommendations, options analysis, and decision frameworks.
    agent_id: strategist-01
    agent_role: strategist
    stagger_offset_seconds: 450.0
    system_prompt: |
      You are a strategist agent. Produce actionable recommendations in markdown.
      Structure your output as follows:
      - **Situation**: Brief summary of the context and constraints.
      - **Options**: List 2-4 concrete options with pros and cons for each.
      - **Recommendation**: State the recommended option and rationale clearly.
      - **Risks**: Identify top 2-3 risks and mitigation approaches.
      - **Next Steps**: Numbered list of immediate actions to execute the recommendation.
      Be direct and decisive. Avoid hedging language. Write for decision-makers.
    tool_allowlist: []  # No tools in Phase 4; populated in Phase 5
    ```

    Verify each file can be loaded via load_agent_config(role_path, cluster_config_path=cluster_path) without ValidationError.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
from runtime.config import load_agent_config
base = Path('config/cluster.yaml')
for role in ['researcher', 'writer', 'strategist']:
    cfg = load_agent_config(Path(f'config/agents/{role}.yaml'), base)
    assert cfg.agent_role == role, f'Expected role={role}, got {cfg.agent_role}'
    assert cfg.db_path == 'db/cluster.db', f'cluster.yaml merge failed for {role}'
    assert cfg.system_prompt, f'system_prompt empty for {role}'
    assert isinstance(cfg.tool_allowlist, list)
print('All 3 role configs load and merge correctly')
"</automated>
  </verify>
  <done>All three role YAML files exist. Each loads and merges with cluster.yaml via load_agent_config() returning a valid AgentConfig with role, db_path, system_prompt, and tool_allowlist populated.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite coverage gate</name>
  <files></files>
  <action>
    Run the full test suite with coverage. All 18 test_worker.py tests must be GREEN (no xfail remaining from stubs). Address any remaining gaps:

    1. Run: `pytest tests/test_worker.py -v` — identify any still-failing tests
    2. For each failing test, diagnose and fix the implementation in runtime/worker.py
    3. Run: `pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80`
    4. If coverage < 80%, identify uncovered lines and either add targeted tests or confirm they are unreachable branches

    Common gaps to check:
    - Exception path in _execute_task (LLM raises) — may need a test for ERROR state
    - Lost-race path in _claim_next_task (rowcount = 0) — should be covered by W-07
    - Empty task list path in do_own_tasks — covered by W-18

    If coverage between 80-89%: acceptable, ship it.
    If coverage >= 90%: ideal.
    Do NOT add tests purely to inflate coverage numbers — only add tests for real behaviours.

    Final commit: `docs(04-05): create role YAML configs + confirm full suite GREEN`
  </action>
  <verify>
    <automated>pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80</automated>
  </verify>
  <done>All 18 W-requirement tests GREEN. Full suite passes with coverage >= 80%. Phase 4 deliverables complete.</done>
</task>

</tasks>

<verification>
`pytest tests/test_worker.py -v` — 18 tests GREEN (0 xfail, 0 skip).
`pytest --cov=runtime --cov-report=term-missing --cov-fail-under=80` — full suite passes.
`python -c "from runtime.worker import WorkerAgent; print('import OK')"` — no ImportError.
</verification>

<success_criteria>
- config/cluster.yaml, config/agents/researcher.yaml, writer.yaml, strategist.yaml all exist
- All three role configs load and merge correctly with cluster.yaml via load_agent_config()
- All 18 test_worker.py tests GREEN
- pytest --cov passes at 80%+ coverage
- No regression in phases 1-3 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/04-worker-agents/04-05-SUMMARY.md`
</output>
